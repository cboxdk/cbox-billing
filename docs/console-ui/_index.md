---
title: Console UI
description: The reusable console UX conventions every provider-console screen follows — the confirm dialog, pagination partial, breadcrumb component, accessible table rows, flash banner, and responsive utilities — so new surfaces stay consistent and safe by default.
weight: 65
---

# Console UI

The provider console is built from a small, fixed set of reusable patterns on the shared
`cbx-*` design tokens. New screens compose these rather than inventing markup, so every
list searches and paginates the same way, every destructive action is guarded the same way,
and every page is keyboard-operable and responsive without per-screen effort.

- **[Reusable patterns](reusable-patterns.md)** — the confirm dialog, pagination partial,
  breadcrumb component, accessible row pattern, flash banner, and responsive utilities, with
  the exact snippet to copy for each.
