<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <!--
            SEC-1 console operator-org gate. The suite signs into the console as principals in
            these operator orgs, so the gate stays ACTIVE (deny-by-default is NOT disabled) while
            the existing console tests still reach it. Tests that assert the deny path override
            this via config()->set() to an empty allowlist. Keep in sync with the orgs used in
            the console feature tests' session fixtures.
        -->
        <env name="CBOX_BILLING_OPERATOR_ORGS" value="Cbox Systems,org_hverdag,Cbox"/>
        <!-- Federated RBAC enforcement is opt-in; pin it OFF so the suite is hermetic
             regardless of a developer's local .env (the console tests exercise the
             default inert gate — the enforced path has its own dedicated coverage). -->
        <env name="CBOX_ID_RBAC_ENFORCE" value="false"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <env name="TELEMETRY_ENABLED" value="false"/>
        <env name="HEALTH_CACHE_ENABLED" value="false"/>
    </php>
</phpunit>
